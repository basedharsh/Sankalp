import 'package:flutter/material.dart';

class volChatPage extends StatelessWidget {
  const volChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Chat Page"),
      ),
    );
  }
}
