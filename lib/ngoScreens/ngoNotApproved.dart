import 'package:flutter/material.dart';

class NgoNotApproved extends StatelessWidget {
  const NgoNotApproved({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Not Approved'),
      ),
    );
  }
}
